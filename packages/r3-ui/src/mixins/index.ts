/**
 * Barrel for opt-in behavior mixins: `createMixin`-based building blocks,
 * one module per behavior, that attach a `behaviors/` state model to a host
 * element through the standard `mix` prop alongside `css()` and friends.
 *
 * @author [Sergio Xalambrí](https://sergiodxa.com)
 * @copyright Sergio Xalambrí 2026
 */

export {};
