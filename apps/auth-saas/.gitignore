# Artifacts the root .gitignore does not already cover.

# Folders
/.wrangler
# Browser and tenant bundles emitted here by `bun run build` and served by the
# ASSETS binding; every file under it is generated, so the whole folder is ignored.
/assets

# Files
.dev.vars
worker-configuration.d.ts
