/**
 * Web route table for the r3-uptime app. It exports the route definition consumed by
 * the fetch-router in the application bootstrap, currently an empty route map that
 * serves as the placeholder to be filled in as HTTP endpoints are added. It exists
 * as the central declaration of the app's public URL surface.
 *
 * @author [Sergio Xalambrí](https://sergiodxa.com)
 * @copyright Sergio Xalambrí 2026
 */

import { route } from "remix/fetch-router/routes";

/**
 *
 */
export default route({});
