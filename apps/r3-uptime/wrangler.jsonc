{
	"$schema": "https://unpkg.com/wrangler@latest/config-schema.json",
	"name": "r3-uptime",
	"main": "./bootstrap/worker.ts",
	"compatibility_date": "2026-04-10",
	"compatibility_flags": ["nodejs_compat"],
	"workers_dev": true,
	"dev": { "port": 3000 },
	"placement": { "mode": "smart" },
	"observability": { "enabled": true },
	"assets": { "directory": "./build/client" },

	"triggers": {
		"crons": [
			"* * * * *", // Every minute - uptime monitors + cron job monitors
			"*/5 * * * *", // Every 5 minutes - TCP monitors
			"*/10 * * * *", // Every 10 minutes - domain verification
			"0 * * * *", // Every hour - DNS monitors
			"0 0 * * *", // Every day at midnight - cleanup
			"0 1 * * *", // Every day at 1 AM UTC - aggregate daily stats
			"0 6 * * *", // Every day at 6 AM UTC - SSL checks
		],
	},

	// Custom domain route moves here at Phase 10 cutover
	"routes": [],

	// Plain secrets (COOKIE_SESSION_SECRET, CLIENT_ID/SECRET, POLAR_ACCESS_TOKEN,
	// RESEND_API_TOKEN, UPTIME_CRON_API_KEY, CLOUDFLARE_ACCOUNT_ID/ANALYTICS_TOKEN) are
	// set via `.dev.vars` locally and `wrangler secret put` in production — same as the
	// OLD APP (`apps/uptime/app/cookies.ts` reads `env.COOKIE_SESSION_SECRET` directly)
	// and `apps/auth-saas`. Not declared here; only bindings need a wrangler.jsonc entry.

	"kv_namespaces": [
		{
			"binding": "KV",
			"id": "ea5b4a1bb7804692992b013e1627a8ad",
			"preview_id": "ea5b4a1bb7804692992b013e1627a8ad",
		},
	],

	"d1_databases": [
		{
			"binding": "DB",
			"database_name": "ping",
			"database_id": "b51fff7c-c4dd-412d-8b56-57da405b780e",
			"preview_database_id": "b51fff7c-c4dd-412d-8b56-57da405b780e",
			"migrations_dir": "./database/migrations",
		},
	],

	// R2 bucket bindings can be added here when needed
	"r2_buckets": [],

	// Only a producer until Phase 10 cutover: a Cloudflare queue can have a single
	// consumer worker, and the OLD APP (`ping`) still consumes it until then.
	"queues": {
		"producers": [{ "binding": "QUEUE", "queue": "ping" }],
	},

	"durable_objects": {
		"bindings": [{ "name": "GEO_FETCH", "class_name": "GeoFetchDO" }],
	},
	"migrations": [{ "tag": "add-geo-fetch-do", "new_classes": ["GeoFetchDO"] }],

	"workflows": [{ "name": "ping-workflow", "binding": "PING", "class_name": "Ping" }],

	"analytics_engine_datasets": [{ "binding": "PING_RESULTS", "dataset": "uptime_monitor_results" }],
}
